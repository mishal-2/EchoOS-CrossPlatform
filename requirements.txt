# Core GUI Framework
PySide6>=6.5.0

# Speech Recognition and Audio
vosk>=0.3.44
sounddevice>=0.4.6
# PyAudio is optional - sounddevice is the primary audio library

# Text-to-Speech
pyttsx3>=2.90

# Voice Authentication and Processing (Optional - graceful degradation)
# resemblyzer>=0.1.1.dev0
# librosa>=0.10.0
# python_speech_features>=0.6

# Scientific Computing
numpy>=1.24.0,<2.0.0
scipy>=1.10.0

# Text Processing
rapidfuzz>=3.0.0

# System Information and Control
psutil>=5.9.0

# Encryption and Security
cryptography>=41.0.0

# Platform-specific Dependencies
pywin32>=305; platform_system == "Windows"
pyobjc-framework-Cocoa>=9.0; platform_system == "Darwin"
pyobjc-framework-ApplicationServices>=9.0; platform_system == "Darwin"

# Accessibility Features (Optional)
pyautogui>=0.9.54
Pillow>=10.0.0

# Configuration Management
pyyaml>=6.0.1

# HTTP requests for model downloads
requests>=2.31.0

# Note: Standard library modules used (no installation needed):
# - pathlib, threading, queue, logging, datetime, json, pickle
# - sys, os, platform, subprocess, time, re, hashlib
